server:
  #端口号
  port: 8091
  undertow:
    io-threads: 16
    worker-threads: 256
    buffer-size: 1024
    buffers-per-region: 1024
    direct-buffers: true
spring:
  application:
    name: CoreExample

source:
  enable: true